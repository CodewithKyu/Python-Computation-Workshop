#Ludo dice 

#The randint() function is a part of Python's random module, used to generate a random integer within a specified range

import random

# random.randint(lower bound, upper bound)
r = random.randint(1,6)
print(r)
